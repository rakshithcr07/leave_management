_schema-version: 3.3.0
ID: leave_management
description: A simple CAP project.
version: 1.0.0
modules:
- name: leave_management-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: leave_management-auth
  - name: leave_management-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: leave_management-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: leave_management-db
  parameters:
    buildpack: nodejs_buildpack
- name: leave_management-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: leave_management-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - leavemanagement.zip
      name: leavemanagement
      target-path: resources/
- name: leavemanagement
  type: html5
  path: app/leave_management
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: leave_management-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: leave_management-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: leave_management-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: leave_management-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: leave_management-html5-service
    service-plan: app-host
- name: leave_management-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: leave_management-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
